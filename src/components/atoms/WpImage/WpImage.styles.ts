import styled from 'styled-components';
import { GatsbyImage } from 'gatsby-plugin-image';

export const StyledGatsbyImageWrapper = styled(GatsbyImage)`
  max-width: 100%;
  max-height: 100%;
  height: 100%;
  width: 100%;
`;

export const StyledImageWrapper = styled.img`
  max-width: 100%;
`;
